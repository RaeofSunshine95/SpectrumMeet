@model IEnumerable<SpectrumMeetEF.ParentChild>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.isPrimary)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Account.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Child.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.isPrimary)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Account.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Child.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParentChildID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParentChildID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParentChildID })
        </td>
    </tr>
}

</table>
